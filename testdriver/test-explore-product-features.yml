version: 4.0.78
steps:
  - prompt: Hover over "Product" in the header.
    commands:
      - command: hover-text
        text: PRODUCT
        description: link in the header
        action: hover
  - prompt: Click "Features."
    commands:
      - command: hover-text
        text: FEATURES
        description: link in the header
        action: click
  - prompt: Click "Features."
    commands:
      - command: hover-text
        text: FEATURES
        description: link in the header
        action: click
  - prompt: Click "Features."
    commands:
      - command: hover-text
        text: FEATURES
        description: link in the header
        action: click
